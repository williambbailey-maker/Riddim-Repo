/* Riddim Repo backend configuration.
   The publishable key is safe to ship in client code — all data access
   is enforced server-side by Row Level Security. */

window.RIDDIM_CONFIG = {
  supabaseUrl: 'https://dywrisybvcorpfhbwgtg.supabase.co',
  supabaseKey: '__PENDING_KEY__',
};
